# function to place n objects in 2x(n/2) grid in area of size [W x H]
import numpy as np

def gridButton(n, dims):
    nRows = n / 2
    
    return nRows

def quitButton(dims):
    xd = max(dims)
    yd = max(dims)
    return [xd, yd]